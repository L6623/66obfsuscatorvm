<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Protected Loadstring</title>
    <style>
        body {
            margin: 0;
            height: 100vh;
            background: #0a0a0a;
            color: #e0e0e0;
            font-family: system-ui, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
        }
        .lock { font-size: 120px; margin-bottom: 20px; }
        h1 { color: #ffcc00; font-size: 3.5rem; margin: 10px 0; }
        .msg {
            background: #1c1c1c;
            border: 2px solid #444;
            padding: 40px 60px;
            border-radius: 16px;
            max-width: 700px;
            box-shadow: 0 0 50px rgba(0,0,0,0.9);
        }
        .big-text {
            color: #ff4444;
            font-size: 2.2rem;
            font-weight: bold;
            margin: 20px 0;
            text-transform: uppercase;
            letter-spacing: 3px;
        }
        .small { color: #888; font-size: 1.3rem; margin-top: 30px; }
        footer { margin-top: 60px; color: #555; font-size: 1rem; }
    </style>
</head>
<body>
    <div class="lock">🔒</div>
    <h1>Loadstring</h1>
    
    <div class="msg">
        <div class="big-text">This File Was Protect By luaobf</div>
        <div class="small">Protected content — Browser view blocked</div>
        <div class="small" style="margin-top:25px;">Use only in executor with HttpGet</div>
    </div>

    <footer>Contents cannot be displayed in browser • luaobf protected</footer>
</body>
</html>
